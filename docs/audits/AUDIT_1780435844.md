# AttestMesh Project Audit (CSK P2P-distribution consistency pass)

**Timestamp**: 1780435844 (2026-06-02)
**Scope**: `/home/ubuntu/teemesh` — full project
**Project state**: spec-only (master + 4 component specs + 4 component READMEs + holodeck scaffolding); 0 code. Follows commit `c43437a` (CSK distribution → peer-to-peer pull + on-chain commitment); master now Draft v0.4.
**Audit method**: 3 parallel read-only agents — (A) CSK-change deep consistency, (B) cross-spec + prior-invariant regression, (C) docs drift — plus build/tests/lint (N/A: no code yet).

---

## Summary

**Overall Health**: EXCELLENT — the CSK on-chain→P2P migration is internally consistent end-to-end, the old on-chain path is fully removed, and no prior-audit invariant regressed. One MEDIUM proto/prose mismatch fixed this pass; one README drift handed to the `/reconcile all` running immediately after.

| Category | Status | Issues |
|---|---|---|
| Build / Tests / Lint | N/A | no code |
| CSK change (internal) | PASS | 1 MEDIUM (fixed this pass) |
| Cross-spec / regression | PASS | 0 (1 INFO) |
| Docs / READMEs | DRIFT | 1 HIGH (deferred to the following reconcile) |

---

## Fixed in this pass (commit `<this commit>`)

### F1. [MEDIUM] master §8.2 prose dropped the `CskRequest` wrapper

`attestmesh-coordination-layer.md` §8.2 step 1 wrote `PeerControl.RequestClusterSharedKey(self.memberId)`, but the canonical proto (sidecar spec §12.5) is `RequestClusterSharedKey(CskRequest{ requester_member_id })`. **Fixed**: master now calls `RequestClusterSharedKey(CskRequest{ requester_member_id: self.memberId })`, matching the proto and the sidecar onboardee path.

---

## Deferred to the immediately-following `/reconcile all`

### R1. [HIGH — docs↔spec drift] README CSK bullet is stale

`README.md:21` still describes CSK distribution as "sealed-box-encrypted on-chain envelopes with built-in dedup" — contradicts master §8.2/§8.3 (now peer-to-peer pull + a one-time `keccak256(CSK)` commitment; no envelopes, no `DuplicateEnvelope` dedup). Left for the reconcile pass (docs↔spec is reconcile's domain; the corrected wording is already drafted).

---

## CSK migration verification — PASS

- **Names identical** across master / contracts / sidecar / gas-webhook: `setCskCommitment(bytes32)`, `cskCommitment() → bytes32`, `CskCommitmentSet(bytes32)`, and `PeerControl.RequestClusterSharedKey(CskRequest) → SealedCsk`.
- **Originator-gating** consistent: commitment set-once by the first registrant (`memberIds[0]`); errors `NotOriginator()` / `CskCommitmentAlreadySet()` declared.
- **End-to-end pull flow** coherent: originator derives → seals → `setCskCommitment(keccak256(csk))`; onboardee pulls once a tunnel is up → sealed-box open (x25519) → verify `keccak256(csk) == cskCommitment()` → seal; serve side (verify requester membership, return sealed CSK or `Unavailable`) matches. master §8 and sidecar CSK appendix agree on all four paths.
- **Old on-chain path fully removed**: zero `attestmesh.csk.onboarding.v1`, MessageFacet-carried CSK, `[0,500]ms` backoff, or push-onboarding references in any spec.
- **Cross-refs resolve**: master §8.2 → sidecar §12.5 (exists, matches); gas-webhook allowlist includes `setCskCommitment(bytes32)`.
- **Commitment value** is `keccak256(csk)` everywhere.

## Cross-spec / prior-invariant regression — PASS

- `cskCommitment` ABI agrees master ↔ contracts (storage field, setter, view, event, errors).
- Namespaces consistent; `bind.v1` (message domain) vs `binding.v1` (derive-key purpose) distinction intact; `attestmesh.peer-endpoint.v1` is the sole remaining on-chain reserved envelope.
- Proto: indexer §8.1 ↔ sidecar §9.1 match field-for-field; the new `proto/peer.proto` (sidecar §12.5) is self-consistent.
- gas-webhook allowlist (17 entries) all map to real diamond/ClusterMember functions.
- All AUDIT_1780358114 invariants hold — `attestorId` (no `platformId`), `memberIds`, `meshCidr` split fields, packed CIDR `168624128`/`0x0a0d0000`, the join invariant, the §4.1 method-agnostic key-binding framing. **No regression.**

## INFO (accepted, no action)

- `contracts.md:690` — IndexerRegistry example endpoint `indexer.teesql.io` is a stale pre-rebrand placeholder (a `teesql.io` host, not a `teemesh` residue). Same INFO as the prior audit; left as an illustrative placeholder.

---

## Recommendations

The spec tree remains internally consistent and ready for code generation. The `/reconcile all` running next clears R1. As before, a fresh audit once the first code lands will be more valuable than another spec-only pass. Retention: this report replaces `AUDIT_1780358114.md`.
