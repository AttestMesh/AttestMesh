# TeeMesh Project Audit (re-audit after AUDIT_1780176327 fixes)

**Timestamp**: 1780179715 (2026-05-30)
**Scope**: `/home/fbx/teemesh` — full project
**Project state**: spec-only (4 component specs + master spec + holodeck scaffolding); 0 commits to code, 21 commits to specs
**Audit method**: 2 parallel agents — master-spec internal pass + cross-spec consistency pass — running the same checks as the prior audit (AUDIT_1780176327) plus a fresh pass for fix artifacts and regressions

---

## Summary

**Overall Health**: EXCELLENT — *no remaining HIGH-severity issues; spec tree is internally consistent and ready for code generation.*

| Category | Status | Issues |
|---|---|---|
| Build / Tests / Lint | N/A | no code yet |
| Specs (internal) | PASS | 0 residual + 0 regression + 0 new |
| Specs (cross-spec) | PASS | 0 residual + 0 regression + 0 new |
| Docs / file refs | INFO | unchanged from prior audit |

The prior audit identified 11 HIGH + 17 MEDIUM + 7 LOW issues. 30 of 35 were fixed in commit `9035615`; the 5 left alone (2 LOW deferred per prior recommendation, 3 INFO are positive findings).

The re-audit caught **6 residuals + 2 new fix-related items** — all variations of the same root issues that didn't propagate to every mention — and they have been fixed in commit `<this commit>`. After that fix-the-fixes pass, the re-audit reports 0 outstanding issues.

---

## Residuals from AUDIT_1780176327 (all fixed in this pass)

### R1. H4 not fully propagated — three places still claimed "one-shot" binding key

- `teemesh-coordination-layer.md:485` (§13 item 2): "after registration that key is never used again" — directly contradicts the §4.2 / §9 fixes from the prior pass. **Fixed**: rewrote item 2 to acknowledge the key is reused for every subsequent UserOpHash.
- `teemesh-coordination-layer.md:435` (§10): "no per-CVM ECDSA touches the chain" — pre-4337 framing. **Fixed**: rewrote the dstackgres-difference bullet to acknowledge the key reuse for UserOpHash signing.
- `teemesh-coordination-layer.md:421` (§9 trust model): "On future platforms... no per-CVM ECDSA at all" — true for the *attestation* binding but those platforms still need a signing key for EIP-4337 UserOps. **Fixed**: scoped the claim to the attestation binding specifically and added the explicit note about UserOp signing on future platforms.

### R2. H3 not fully propagated — two places still called ClusterMember a "passthrough"

- `teemesh-coordination-layer.md:90` (§2 architecture overview prose): "ClusterMember passthrough contract". **Fixed**: rewrote to describe the dual-role dstack-proxy + EIP-4337-smart-wallet shape.
- `teemesh-coordination-layer.md:50` (§2 ASCII diagram arrow label): "passthrough". **Fixed**: arrow now reads "execute() (EIP-4337 entry path) + dstack IAppAuth"; diagram box now labels the ClusterMember as "dstack app proxy + EIP-4337 smart wallet."

### R3. M4 not fully propagated

- `teemesh-coordination-layer.md:327` (§7.3): still called `DiamondInit.InitArgs.meshCidr` a single field. **Fixed**: now correctly references the `(meshCidrIp, meshCidrPrefix)` pair.

### R4. Obsolete self-correcting footnote in contracts spec

- `contracts.md:490`: "(`MemberStorage.Layout` gets an additional `address clusterOwner` field for this; corrects §4.1 above.)" — this footnote was added when §4.1 didn't yet have the field; §4.1 has been the canonical layout since the prior fix pass. **Fixed**: footnote deleted.

### R5. Master spec §6.4 step 4 used non-proto envelope field names

- `teemesh-coordination-layer.md:267` listed envelope fields as `{event, clusterAddress, blockNumber, ...}` — the proto field names are `event_data`, `cluster_addr`, `block_number`. **Fixed**: updated to match the canonical proto field names from indexer.md §8.1 and sidecar.md §9.1.

---

## NEW items found in the re-audit (all addressed)

### N1. (cosmetic) Proto excerpts use illustrative pseudo-types

Both `sidecar.md:264` and `indexer.md:243` write `bytes32`, `bytes64`, `uint32` in the proto3 excerpts. Proto3 doesn't have these as built-in types — actual codegen will use `bytes` with length comments. **Decided**: Both specs already note that the proto file `sidecar/proto/indexer.proto` is the canonical codegen source and the inline excerpts are illustrative. No action; this is documented behavior.

### N2. Future-platform ECDSA claim was internally inconsistent

§9 trust model claimed "no per-CVM ECDSA at all" for future raw-quote platforms; the platforms still need a UserOp signer. **Fixed** as part of R1's third bullet (above).

---

## Cross-spec verification — all 14 prior HIGH/MEDIUM items confirmed resolved

| Item | Status |
|---|---|
| H1 `teemesh.bind.v1` vs `teemesh.binding.v1` | ✓ Both spellings appear correctly with the distinction documented at master §4.2 + sidecar §6 |
| H2 `wgPublicKeyOf` → `wgPubKeyOf` | ✓ No stale `wgPublicKeyOf` anywhere |
| H7 PushEnvelope `event_data` | ✓ Both protos use `bytes event_data = 1` |
| H8 `RpcReproStub` / `IndexerAttestation` | ✓ sidecar.md cross-refs indexer.md §8.1 as canonical |
| H11 `acceptOwnership()` in allowlist | ✓ Present in gas-webhook.md §7 |
| M1 IAttest declarations | ✓ All four selectors + MemberRegistered event declared |
| M2 MemberStorage.Layout consolidated | ✓ One canonical definition at contracts.md §4.1 |
| M3 `memberByAddress` → `memberIdOf` | ✓ Only `memberIdOf` appears |
| M4 `meshCidr` split fields | ✓ Storage uses two fields; getter returns tuple |
| M5 `dstack.derive_key` | ✓ No stale `dstack.get_key` |
| M12 `DeployInfra.s.sol` in file layout | ✓ Listed |
| M13 `MEMBER_PROVENANCE_CACHE` KV namespace | ✓ Declared alongside `FACTORY_PROVENANCE_CACHE` |
| M14 `ENTRY_POINT` constant vs immutable | ✓ Declared as `constant` |
| M15 `MemberRemoved` milestone-B gate | ✓ Gated in indexer.md §7.3 |

---

## Detail-pass observations (informational, no action)

- **All cross-spec relative links resolve.**
- **Selector allowlist (16 entries in gas-webhook.md §7)**: every entry exists on the diamond per `contracts.md`. Bootstrap (cluster Safe `acceptOwnership` after deploy) is sponsorable.
- **Bootstrap-UserOp story matches** between `contracts.md` §9.1.2 (two-mode validateUserOp) and `sidecar.md` §8.3 (sidecar's view of the bootstrap path).
- **Indexer push envelope shape** is identical between `indexer.md` §8.1 (canonical) and `sidecar.md` §9.1 (excerpted).
- **Reserved envelope ids** consistent across master §7.1 / §8.2 / contracts.md §5.2 / sidecar.md §13.
- **Key derivation purpose strings** consistent: `teemesh.identity.v1`, `teemesh.wireguard.v1`, `teemesh.binding.v1`, `teemesh.cluster-shared.v1`, `teemesh.indexer.signing.v1`, `teemesh.csk.v1`.
- **Env var names** consistent across sidecar/indexer/gas-webhook.
- **18 resolved decisions in master §13** are internally consistent and do not contradict the main body.

---

## Recommendations

The spec tree is now internally consistent. The natural next step is `/generate` — start with `contracts/` since everything else depends on the ABI.

A third audit after the first code lands will be more useful than another spec-only audit.
